version=1.9.25-SNAPSHOT
kotlin.code.style=official
kotlinVersion=1.9.25

# slf4j-ktx-core — independent cadence from the plugin/Kotlin pair above.
# Stamped into the core JAR's META-INF/MANIFEST.MF so the plugin can read both axes at compile time.
coreVersion=0.1.0-SNAPSHOT
requireKotlin=1.9.25
